#!/bin/sh

env_dir=$1
if [ -d "$env_dir" ]; then
  for e in $(ls $env_dir); do
    export "$e=$(cat $env_dir/$e)"

  done
fi

echo $PROJECT_GIT_URL
echo $PROJECT
git clone $PROJECT_GIT_URL $PROJECT
mv $PROJECT "/app/"
